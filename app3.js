//game state prototyping

gameStart = false;

while (gameStart === true) {
  console.log("how many Players");
  if (players === 2)
    if (player1Hand.length === 0) {
      console.log("You Win");
    }
}
